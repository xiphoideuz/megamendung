{
  "name": "megamendung-web",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "megamendung GUI relay: WhatsApp-Web-style device pairing for the megamendung CLI",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5",
    "typescript": "^5",
    "vitest": "^2",
    "wrangler": "^4"
  }
}